%{
#include <stdio.h>
%}

%%

[0-9]+      { printf("NUMBER: %s\n", yytext); }
[a-zA-Z]+   { printf("WORD: %s\n", yytext); }
\n          { /* ignore newlines */ }
[ \t]+      { /* ignore spaces/tabs */ }

.           { printf("OTHER: %s\n", yytext); }

%%

int main() {
    yylex();
    return 0;
}
